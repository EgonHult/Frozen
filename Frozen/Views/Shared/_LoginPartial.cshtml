
<ul class="navbar-nav">

    @if (User.Identity.IsAuthenticated)
    {
        <li class="nav-item">
            <div class="dropdown">
                @if (!User.IsInRole("Admin"))
                {
                    <button class="btn btn-info btn-sm dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        @User.Claims.Where(x => x.Type == "FirstName").Select(x => x.Value).FirstOrDefault() <i class="far fa-user"></i>
                    </button>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item">Mina ordrar</a>
                        <a class="dropdown-item">Kontoinformation</a>
                        <a class="dropdown-item" asp-controller="LogOut" asp-action="Index">Logga ut</a>
                    </div>
                }
                else
                {
                    <button class="btn btn-info btn-sm dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        ADMIN 666 <i class="far fa-user"></i>
                    </button>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item" asp-controller="OrderAdmin" asp-action="Index">Orderhantering</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" asp-controller="Products" asp-action="ManageProductsPage">Produkthantering</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" asp-controller="LogOut" asp-action="Index">Logga ut</a>
                    </div>
                }
            </div>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Login" asp-action="RegisterPage">Registrera</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Login" asp-action="LoginPage">Logga In</a>
        </li>
    }
</ul>