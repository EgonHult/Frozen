@model List<Frozen.Models.Product>
@{
    ViewData["Title"] = "Produkter";
}

<h1>Produkter</h1>


<div class="row flex-row">
    @foreach (var item in Model)
     {
     <div class="col-sm-12 col-md-6 col-lg-4 mb-5 d-flex justify-content-center">
        <div  class="card text-center shadow rounded-lg border-0 product-image" style="background-color: #f5d7da">
            <div class="rounded product-image">
                <img class="card-img-top" src="@item.Image" alt="bild" width="350" height="250">
            </div>
            <div class="card-body pt-2 pb-0">
                <h5 class="card-title">@item.Name</h5>
                <p class="card-text">@item.Details</p>
                <p class="card-text">@item.Price kr</p>
                <input type="button" value="Lägg i varukorg" onclick="AddToCart('@item.Id')" class="button button1" />
            </div>
        </div>
     </div>
     }
</div>